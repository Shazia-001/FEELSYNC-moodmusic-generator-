
const icons = (
  <>
    
  </>
);

export default function EmotionSlider() {
  return (
    <div className="slider">
      <div className="slide-track">
        {icons}
        {icons}
        {icons}
        {icons}
        {icons}
        {icons}
        {icons}
        {icons}
      </div>
    </div>
  );
}
